<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="io.github.kutaiba_nezar_kashmar.newapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission
            android:name="android.permission.BIND_AUTOFILL_SERVICE"
            tools:ignore="ProtectedPermissions" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme_light">
        <activity
                android:name="io.github.kutaiba_nezar_kashmar.multiverse_of_geeks.MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme_light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="io.github.kutaiba_nezar_kashmar.multiverse_of_geeks.ui.login.SignInActivity" />

        <meta-data
                android:name="io.github.kutaiba_nezar_kashmar.multiverse_of_geeks.API_KEY"
                android:value="${API_KEY}" />

        <meta-data
                android:name="io.github.kutaiba_nezar_kashmar.multiverse_of_geeks.RAWG_API_KEY"
                android:value="${RAWG_API_KEY}" />
    </application>

</manifest>